# The project's automatic renv autoloader path blocks during R startup in the
# working environment used for this project, including Quarto R sessions.
# Disable that automatic path before explicitly sourcing the project activation
# script below; the explicit activation remains the startup mechanism here.
Sys.setenv(RENV_CONFIG_AUTOLOADER_ENABLED = "FALSE")

source("renv/activate.R")
